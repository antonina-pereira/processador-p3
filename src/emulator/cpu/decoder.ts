// Converts machine instructions into microcode entry points
// Reads the opcode from the instruction register
// Maps opcode to microcode start address
// Extracts operand fields (registers, immediates, addressing modes)
// Produces a DecodedInstruction object
